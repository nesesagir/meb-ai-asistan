# Dashboard

Served by the backend at `/dashboard/`.

Demo logins (local only):

- `veli` / `veli1`
- `ogretmen` / `ogretmen1`

The device talks to the backend over WebSocket; the panel reads status and history through the HTTP API.
